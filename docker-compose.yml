services:
  db:
    image: postgres:16
    container_name: Cocoa
    environment:
      POSTGRES_DB: cocoaDB
      POSTGRES_USER: sujitbhatta
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - my_new_project_db:/var/lib/postgresql/data

volumes:
  my_new_project_db: